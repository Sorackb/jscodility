/* eslint-disable no-undef */
// const { solution } = require('./');

const configure = (param, result) => {
  test(`6. Sorting - Maximal Product of Three - ${param} => ${result}`, () => {
    // expect(solution(param)).toBe(result);
  });
};

configure([3, 1, 2, -2, 5, 6], 60);
